Install dependencies and run the development server
Environment variables are required for authentication, database, AI services, and Cloudinary.

npm install
npm run dev

Open http://localhost:3000 to view the app locally
